(()=>{'use strict';
const START=new Date('2025-01-01T00:00:00'),END=new Date('2029-01-01T00:00:00');
let meta=null,timer=null;
const q=s=>document.querySelector(s),all=s=>[...document.querySelectorAll(s)];
function pct(d){return Math.max(0,Math.min(100,(d-START)/(END-START)*100))}
function fmt(v){const d=new Date(v+'T00:00:00');return new Intl.DateTimeFormat('it-IT',{day:'2-digit',month:'2-digit',year:'numeric'}).format(d)}
function stageMap(){return new Map((meta?.maturity_scale||[]).map(x=>[x.code,x.label]))}
function decorateHeading(){const panel=q('.timeline-panel');if(!panel)return;const h=panel.querySelector('.panel-heading h2'),p=panel.querySelector('.panel-heading p');if(h)h.textContent='Calendario attività e milestone';if(p)p.textContent='Lo stage E0–E8 indica lo stato corrente del progetto; le barre mostrano attività documentate o previste nel tempo. Una barra futura non fa avanzare lo stage finché l’avvio non è osservato.';if(!panel.querySelector('.timeline-reading-note')){const note=document.createElement('div');note.className='timeline-reading-note';note.innerHTML='<b>Come leggere i due riquadri insieme:</b> “Stato corrente per maturità” è la fotografia di oggi. Questo calendario mostra quando sono avvenute, sono in corso o sono previste le attività. La linea rossa indica la data di aggiornamento del Radar.';const fallback=q('#timelineFallback');if(fallback)fallback.insertAdjacentElement('beforebegin',note)}}
function addAxis(){const fallback=q('#timelineFallback');if(!fallback||!meta)return;let axis=q('.timeline-axis-row');if(!axis){axis=document.createElement('div');axis.className='timeline-axis-row';fallback.insertAdjacentElement('beforebegin',axis)}const today=pct(new Date(meta.as_of+'T00:00:00'));axis.innerHTML=`<div class="timeline-axis-label">Periodo</div><div class="timeline-axis-track"><span class="timeline-axis-year first" style="left:0%">2025</span><span class="timeline-axis-year" style="left:25%">2026</span><span class="timeline-axis-year" style="left:50%">2027</span><span class="timeline-axis-year" style="left:75%">2028</span><span class="timeline-axis-year last" style="left:100%">2029</span><i class="timeline-today-axis" style="left:${today}%"><span>oggi · ${fmt(meta.as_of)}</span></i></div>`}
function decorateRows(){if(!meta)return;const sm=stageMap(),today=pct(new Date(meta.as_of+'T00:00:00'));all('#timelineFallback .fallback-row').forEach(row=>{const label=row.querySelector('.fallback-label'),track=row.querySelector('.fallback-track');if(label){const text=label.textContent||'',m=text.match(/\b(E[0-8])\b/),mw=text.match(/([0-9.,]+)\s*MW/i),name=label.querySelector('b')?.textContent||'';if(m&&name){const code=m[1],stage=sm.get(code)||code;label.innerHTML=`<b>${name}</b><span class="timeline-stage-meta"><strong>${code} · ${stage}</strong><span>${mw?mw[1]+' MW':''}</span></span>`}}if(track&&!track.querySelector('.timeline-today-line')){const line=document.createElement('i');line.className='timeline-today-line';line.style.left=today+'%';line.title='Data di aggiornamento Radar: '+fmt(meta.as_of);track.appendChild(line)}})}
function renameSnapshot(){const panel=q('.pipeline-panel');if(!panel)return;const h=panel.querySelector('.panel-heading h2'),p=panel.querySelector('.panel-heading p');if(h)h.textContent='Stato corrente per maturità';if(p)p.textContent='Fotografia dei progetti sulla scala E0–E8 alla data di aggiornamento. I MW BESS non sono sommati all’eolico.';const chart=q('#pipelineChart');if(chart)chart.setAttribute('aria-label','Stato corrente dei progetti per maturità')}
function refresh(){if(!meta)return;renameSnapshot();decorateHeading();addAxis();decorateRows()}
function schedule(){clearTimeout(timer);timer=setTimeout(refresh,60)}
async function init(){try{meta=await fetch('data/meta.json',{cache:'no-store'}).then(r=>r.json());refresh();new MutationObserver(schedule).observe(document.body,{childList:true,subtree:true})}catch(err){console.error('timeline-coherence init failed',err)}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();